{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认 capability — 主窗口的基础权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fiona-desktop:allow-open-url"
  ]
}
