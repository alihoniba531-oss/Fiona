{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "仅供打包启动页使用的本地诊断 capability",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "allow-startup-diagnostics",
    "allow-probe-backend",
    "allow-open-logs-dir"
  ]
}
