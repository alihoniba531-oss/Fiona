# Fiona / Chloe 当前状态与路线图

最后同步：2026-09-05

本文只记录当前实现、发布边界和下一步工作。最初的“天人合一私人助理”构想仍可通过 Git 历史查看，但其中的 Next.js 14、Claude、DuckDuckGo、Docker/Vercel 和未来功能描述已经不再代表现状。

## 产品定位

Fiona 的目标已确认调整为“每个用户拥有自己的 AI 分身，分身可交流并安装或交易 Skill”。当前仍为受控内测架构，完整目标、后续阶段和验收见 [平台方案](./docs/CYBER_AVATAR_PLATFORM.md)。

### 2026-09-05：平台第一阶段已落地

- 每账号一个独立分身：名称、头像符号、简介、人格设置和默认私有的名片；新增 `/agents/me` 与登录后的名片详情页。
- 持久私有会话：创建、自动命名、切换、删除和刷新恢复；聊天历史、工具补参与模式按会话 ID 隔离。
- 启动时版本化事务迁移旧账号和消息，非法归属阻止迁移；旧客户端 `/chat` 省略会话 ID 仍可使用默认会话。
- 分身私有记忆独立于旧社交画像；新会话停止自动匹配和广场标签传播。查看/清空记忆可用，修订号防止在途提取回填。
- 公开名片使用白名单，聊天入口明确 AI 身份；会话删除与账户删除继续清理关联媒体和临时状态。
- 后端全套 113 项测试通过；前端 TypeScript、Lint（0 错误、38 个现存警告）及 Webpack 生产构建通过。
- 使用临时数据库和模拟模型完成浏览器联调：分身编辑/保存、两段独立会话、切换历史、刷新恢复，以及跨标签页切换账号后清除旧分身资料。随后本地预览已切换到持久数据库与真实模型，保留已有分身与会话；未部署到公网。
- 后续修复了上游流的空 choices 帧和错误提示，后端测试增至 127 项；聊天滚动按实际内容高度跟随，长回复、多行输入、上翻暂停、恢复跟随和切换会话已通过浏览器验证。

### 2026-09-05：第二阶段分身交流已实现

- 新增“分身广场”原生抽屉与 `/agents` 页面：发现公开名片、发送主题邀请、处理收发邀请、查看双分身回复与总结。
- 双方名片公开且受邀者接受后开始；默认双方合计 6 次回复，可选 2–6 次，结束后一次总结。任何一方可停止，关闭公开名片同时终止待处理和进行中的交流。
- 服务端记录授权、调用预留与用量，限制重复邀请、并发运行和总调用次数；进程重启停止旧任务并估算未决用量，不重放模型调用。
- 生成上下文只含公开名片与本次交流，私有人格、记忆和聊天不会进入；停止、撤销或删号后的迟到回复与总结不能回填。
- 后端全套 163 项测试通过；隔离浏览器验证六次回复、总结、刷新恢复与中途停止。独立临时数据库下真实模型完成两次回复与一次总结，共 3 次调用，记录了实际用量。
- 前端 TypeScript、所改组件 ESLint 和生产构建通过。本地数据库备份后已完成 M2 迁移，当前 3000/8000 服务已更新；已有分身设置与私聊保留，没有自动公开任何名片。
- 当前内测不扣草莓、不调用工具；Skill 安装和交易尚未实现。下一阶段为官方 Skill 发布、免费安装与真实执行，之后接入付费许可和作者收益。

### 2026-09-05：单人分身体验已实现

- 分身广场默认进入“单人体验”，提供“创意搭档”和“脚本编辑”两位平台官方 AI；选搭档、填话题、点击开始即可讨论，无需第二个账号或公开自己的分身。
- 官方角色由服务端注册表提供，不创建虚构用户；体验记录仅发起人可见，和真人分身邀请分别展示。上下文仅含分身名字、简介和本次话题与回复，不读取私聊、性格设定或私有记忆。
- 默认六次分身回复和一次总结，可随时停止；复用原有调用预算、账号并发限制、重启恢复和迟到回复拦截。事务迁移前已备份本地数据库，既有分身、会话与消息逐表比对保持一致。
- 后端全套 190 项测试、前端 TypeScript、所改组件 ESLint 和 Webpack 生产构建通过。隔离浏览器验证两位官方搭档、六次回复、总结、停止及刷新恢复。
- 已用当前 Chloe 与“创意搭档”通过真实模型完成一条 30 秒赛博数字人短视频讨论，六次回复和总结共七次调用均成功并记录实际用量；刷新后可从“体验记录”重新查看。本地 3000/8000 已更新，尚未部署公网。

### 2026-09-05：99 次体验与独立官方模型

- 两位官方搭档默认双方合计 99 次回复，允许输入 2–99 的整数，最后另生成总结；真人分身邀请仍为 2–6 次。上下文包含全部轮次，超长时才对每条取节选，预算支持 99 次回复加总结。
- 本地“创意搭档”和“脚本编辑”已切换 DeepSeek V4 Pro，自己的分身发言继续使用 Qwen3.8 Max；总结由官方模型生成。后端环境变量可独立切换官方模型，卡片显示模型名称，新调用记录供应商与模型，旧记录不推断来源。
- 2026-09-21：主力对话槽由 qwen3.8-max 切换为 qwen3.8-omni-flash；轻量槽、视觉、搜索、生图、语音与官方搭档模型不变。分身交流中自己的分身随主力槽一起切换。
- V3 事务迁移前已停止写入并备份，迁移后逐字段比对分身、私有会话、消息、旧交流与调用记录均保留。密钥只保存在忽略版本控制的服务端配置中。
- 213 项后端测试通过，覆盖完整 99 次加总结、轮次边界、预算、停止后迟到响应、模型路由和数据迁移。前端 TypeScript、所改组件 ESLint 与生产构建通过，当前页面显示默认 99 次和 DeepSeek V4 Pro。
- 真实模型验证使用独立临时数据库完成 8 次交替回复和总结：Qwen 发言 4 次，DeepSeek 发言 4 次并总结 1 次，9 次调用均成功，实际用量 7,855 tokens。另已验证 DeepSeek 官方模型目录与一次简短回复。当前账号正在填写的话题未被修改。

### 2026-09-05：短视频创意搭档

- 官方目录新增“短视频创意搭档”，使用 DeepSeek V4 Pro，默认最多 99 次回复；围绕原创点子、开头钩子、人物冲突、反转和分镜共同完善剧本。
- 服务端按固定角色 ID 加入专属讨论与总结格式，最终交付带连续时码、画面、台词与音效的剧本；普通创意搭档、脚本编辑和真人分身交流继续使用原流程。新增角色不创建虚构用户，也不需要数据库迁移。
- 229 项后端测试通过。真实 Qwen/DeepSeek 在独立临时数据库中完成四次回复与总结；进一步验证的 30 秒分镜时间段连续且合计准确，最终草案包括创意、钩子、反转、画面、台词和音效。

### 2026-09-05：长话题支持

- 单人体验的话题上限从 300 提高到 10,000 字，三个官方角色均适用；前后端共同校验，入库与模型上下文保留完整话题。真人分身邀请仍为 300 字。
- 输入框扩大为八行并显示计数；长话题详情默认预览，可展开、滚动和复制，保持回复区域可访问。
- 234 项后端测试通过，覆盖 10,000 字主题贯穿 99 次回复和总结，以及复杂字符/长历史的输入预算。TypeScript、所改组件 ESLint 和生产构建通过；浏览器确认 10,000 字可完整输入，已把当前页面打开到新版短视频搭档表单。
- 更新前确认没有运行中的讨论；此前用户的 99 次讨论已完成并保存总结。未改写用户话题与记录，后台和前端均已更新。

### 2026-09-05：Markdown 文件交付已实现

- 交流详情提供 `README.md` 和完整讨论 Markdown 两个下载入口。README 包含原始要求、已保存总结、参与分身与状态，并链接到对应的 `discussion-{交流ID}.md`；完整讨论保留每轮发言、原始要求与总结。
- 旧记录也可直接导出；未完成的交流明确标为草稿，只反映已保存内容，不虚构最终交付。导出沿用详情权限：单人体验仅发起人可见，真人分身交流仅双方可见，不读取私有画像或密钥。
- 下载只读取保存的交流快照，不再调用模型，不新增交流记录，也不在服务器创建 Markdown 文件。原始要求和逐轮内容使用动态长度的代码围栏保留，支持完整 10,000 字主题和 99 条回复。
- 后端全套 250 项测试、前端 TypeScript、所改组件 ESLint 和生产构建通过。真实浏览器已打开“墨杀”旧记录，两个下载按钮均请求返回 200，界面显示正常。另通过本地导出接口保存两份 Markdown 至忽略版本控制的 `.local-exports/`，逐条核对完整 1,227 字主题及 99 次回复与数据库一致；浏览器自身的下载保存位置未确认。

### 2026-09-05：作品创作与独立审稿

- 新官方讨论改为“主创完整初稿 → 官方独立审稿 → 主创完整修订”。99 次是上限，审稿通过即可提前结束；最终直接保存已经审过的正文，不再压缩成 200–300 字总结。主创最多 8,192 output tokens，审稿 4,096，取消新流程 1,200 字截断。
- 审稿覆盖交付物、原始约束、一致性、可用性、执行/事实五类；引用可使用真实稿件行号或原文，无法定位的依据不能判通过。保留批评意见，避免格式小差异导致整份审稿丢失。明确的分镜数量与总时长另按实际表行检查，兼容列顺序与每镜/总时长区分，对范围约束不强行等值判断。
- 仅更改版本号或修订日志、同一问题连续三轮未解决、次数耗尽或输出异常时保留待修订稿；AI 审稿通过仍标为待用户验收。保留原授权、预算、停止与迟到结果隔离。
- 详情顶部展示完整作品；新增作品 Markdown 下载，README 也包含全文与审核状态。历史记录保持原样；列表只返回有无作品，避免轮询传输完整正文。
- 后端全套 365 项测试通过，前端 TypeScript、所改文件 ESLint、生产构建通过。真实短剧测试已覆盖完整稿与审稿引用；浏览器使用 Chloe 发起“流程验证”，两次模型调用完成 30 秒六镜剧本（1,539 字），在四次上限前结束，实际用量 6,370 tokens，作品/README/完整讨论通过本地导出接口保存并核对。内置浏览器的自动下载事件未被工具捕获，文件已另行保存到本地可直接打开。
- 本地 3000/8000 已更新。V4 迁移前备份 `backend/local-avatar.db.bak-workflow-20260905-155945`，迁移后逐字段核对 19 张原表，11 条既有交流和 903 条回复全部保留；随后新增一条明确标注的流程验证记录。未部署公网。

原有社交模块的用户旅程（供兼容参考；新私有会话不再自动触发第 4 步）：

1. 使用邀请码登录。
2. 与 Chloe 进行文字、图片或语音对话。
3. 系统保存消息，更新成长状态与结构化画像。
4. 系统从对话需求或长期画像中寻找候选。
5. 用户查看并接受/拒绝匹配卡片。
6. 双方接受后进入真人聊天房间。
7. 用户也可以浏览热点广场、发帖和维护兴趣偏好。

当前不是“用户数量不限”的成熟 SaaS，也没有完成公开社交产品所需的全部安全、隐私和运营能力。

## 已实现

### 对话与模型

- FastAPI SSE 流式聊天。
- Chloe Persona、熟悉度阶段、镜像模式和意图路由。
- `qwen3.8-omni-flash` 主力模型和 `qwen3.8-flash` 轻量槽位。
- `qwen-vl-max` 图片理解。
- 消息历史、用户画像、状态探测和后台提取。
- 草莓余额检查及进程内用量估算。

### 工具与语音

- Qwen 联网搜索、网页卡片、热点分类与展开。
- 视觉搜索、旅行规划、提醒和部分系统/微信辅助工具。
- 浏览器录音、Qwen ASR。
- TTS 普通响应、流式响应和 WebSocket。

### 社交

- 基于消息信号的 Layer 1 匹配。
- 基于长期画像的 Layer 2 匹配。
- 待接受匹配卡片和响应记录。
- 双方接受后的 Peer 房间、历史消息和 WebSocket。
- Plaza 发帖、图片/短视频、点赞、标签和时间偏好。
- Community、Profile、History 和 Settings 页面。

### 客户端与部署

- Next.js 16 / React 19 Web 与 PWA manifest。
- Tauri 2 Windows 桌面壳。
- 桌面开发模式 SSH 隧道和用户模式公网加载。
- Windows GitHub Actions MSI/NSIS 构建。
- 单域名 Nginx + systemd 生产拓扑说明。

## 当前质量基线

2026-08-26 历史审计结果（本轮新增功能验证见上文）：

- 后端：`python -m pytest -q`，84 个测试通过，9 个弃用警告。
- 后端：`compileall` 和 `pip check` 通过。
- 后端：`pip-audit -r requirements.txt`，0 个已知漏洞。
- 前端：`npx tsc --noEmit` 通过。
- 前端：Next.js 16.3.3 生产构建通过（本地沙箱使用 `--webpack`；默认 Turbopack 构建交由 CI 复核）。
- 前端：`npm run lint` 通过，仍有 38 个非阻断警告。
- 前端与桌面 npm 锁定依赖：`npm audit` 均为 0 个已知漏洞。
- 桌面端：CI 有 Windows 构建；当前审计机器没有 Rust/Cargo，因此未做本地编译复核。
- CI：已新增后端/Web 质量门禁，并让 Windows 打包前执行 Rust 单测；工作流尚待推送后的首次 GitHub 运行确认。

这些数字是一次审计快照，依赖或代码变化后必须重新执行命令，不能长期当作通过证明。

## 发布级别

| 级别 | 当前判断 |
|---|---|
| 作者本地使用 | 可用，但仍需保护 `.env`、数据库和上传文件 |
| 受控小范围 Web 内测 | 可继续；扩大范围前完成剩余 P0，并补齐成本与隐私约束 |
| 公开 Web 注册 | 暂不建议 |
| 公开分发桌面安装包 | 条件阻断：代码边界已整改，仍需 Windows/Rust 构建复核、签名和发布验证 |

## 2026-08-26 已完成的第一批修复

### 桌面特权边界

- Rust 命令只接受长度受限、无凭据且带主机名的绝对 HTTP(S) URL。
- Windows 外链改为直接调用系统处理器，不再让 `cmd.exe` 解释 URL。
- 本地页面 capability 只保留诊断、后端探测和日志命令；远程页面只保留两个外链命令。
- Tauri 启动页和 Next.js 页面都已配置 CSP/安全响应头，启动页脚本与样式已外置，不依赖内联脚本、样式或事件处理器。
- 已增加 Rust URL 校验单元测试；因为当前机器没有 Cargo，仍需在 Windows CI 复核编译与运行结果。

### 服务端 URL 请求与 SSRF

- 热点展开和网页卡片统一使用公网 HTTP(S) 客户端。
- 初始 URL、DNS 的全部解析结果和每次重定向都会重新校验；拒绝私网、loopback、链路本地、保留地址、凭据和非默认端口。
- 校验后连接固定到已验证 IP，同时保留 HTTPS SNI 与证书主机名校验，降低 DNS rebinding 风险。
- 已限制重定向次数、超时和响应体大小，并增加 SSRF/重定向/大响应回归测试。

### 匹配、输入与匿名边界

- Layer 2 现在为双方创建卡片；Layer 1、Layer 2 和手动匹配统一校验双方连接偏好。
- 手动匹配只接受候选集合中的用户名，并校验匹配类型、理由、标签数量和长度。
- Chat、图片、ASR、TTS、帖子、分页和总请求体都增加了应用层大小/取值限制；ASR、TTS 和公共热点补充了端点限流。
- 广场匿名标识改为服务端密钥 HMAC；给不存在帖子点赞会返回 404，不再创建孤立点赞记录。
- Plaza 只读端点现在与路由的匿名访问语义一致；前端 `apiFetch` 会统一处理 401 并清理失效会话。
- 3D reduced-motion 状态改为外部订阅，修复了两个 React Effect Lint 错误。

## 2026-08-26 已完成的第二批修复

### 身份、会话与邀请码

- 浏览器会话改为 `HttpOnly`、`SameSite=Lax` Cookie；前端不再把 JWT 写进 `localStorage`，HTTP 媒体和 WebSocket 也不再通过 URL query 传 Token。
- JWT 加入账号会话版本并在每次请求和 WebSocket 握手时核对数据库；退出登录、撤销或轮换邀请码会立即使旧 Token 失效。
- 邀请码兑换改为事务内原子更新，记录首次/最近使用时间和次数；新增 `manage_invites.py` 支持查看、撤销与轮换。
- 邀请码暂时保留可重复登录语义，因为目前没有另一种可用的生产登录凭据；管理员可随时轮换或撤销。

### 完整账户删除

- 新增需要精确输入用户名确认的 `DELETE /account`，设置页提供退出登录和删号入口。
- 单个数据库事务删除用户、消息、画像、成长状态、匹配/待匹配、真人消息、帖子/点赞、兴趣偏好、事件、OTP 和邀请码。
- 广场帖子增加仅服务端可见的真实 owner 字段，并为可识别的旧匿名标识回填归属。
- 删除后不再被引用的上传文件进入持久化清理队列；即时清理失败会在后端启动时及运行期间周期重试。
- 关键后台写入在事务内确认账号仍存在，避免删号与画像/匹配/消息任务并发时重新产生孤立数据。

### 跨用户匹配隐私

- 跨用户候选只向匹配模型提供长度受限的兴趣、价值观、技能、阶段和城市。
- 不再读取或传入候选用户的原始近期消息，也排除需求、困扰、纪念日、职业等敏感画像字段。
- 所有模型评估入口再次执行防御性清洗，避免调用方误传完整画像。

## 2026-08-26 已完成的第三批修复

### 会话、同意和安全底线

- Peer WebSocket 每条消息都会重新核验会话和双方最新接受状态；退出登录会主动断开既有连接，并增加消息大小与发送速率边界。
- TTS WebSocket 在长连接期间复核会话撤销；Peer 历史分页增加硬上限。
- 所有 Persona 阶段统一追加自伤危机、未成年人、非自愿内容、冒充真人和高风险专业建议的基础安全规则；这只是提示词基线，不替代仍待确定的产品政策和代码审核。
- 日志不再写入用户原文、转写结果、失败音频或供应商异常正文；SSE 对上游错误返回通用信息。

### 数据一致性和媒体生命周期

- 删除单条消息、清空历史和删号都会在事务中识别无引用附件，保留仍被广场帖子引用的文件，并使用持久化队列即时/周期重试清理失败项。
- 匹配和待接受卡片增加事务内去重；响应接口不再隐式创建任意匹配，问候语与字段长度也受到校验。
- 聊天与广场图片除魔数/字节上限外，会验证真实格式、完整结构与像素量；损坏内容不会落盘或送入视觉模型。
- 生产数据库和上传目录可通过环境变量移出只读代码目录；部署手册改为专用服务账号、`0600` 密钥文件和加固后的 systemd 权限。

### 依赖和工程门禁

- Next.js 升级至 16.3.3，PyJWT、Pillow、`python-multipart` 升至已修复版本；`shadcn` CLI 从生产依赖删除并只保留项目实际使用的本地样式。
- 前端和桌面 npm 均提交锁文件，三套依赖审计当前均为 0 个已知漏洞。
- 模型客户端统一设置可配置的 60 秒超时和最多 1 次重试；后台任务统一追踪异常并在关机时回收。
- 新增后端/Web GitHub Actions 质量门禁；Windows 工作流改用 `npm ci` 并在 Tauri 打包前运行 Rust 单测。

## P0：剩余发布阻断

### 桌面发布验证

- 在 Windows CI 运行 Cargo 编译和新增 Rust 单元测试。
- 在远程公网模式和本地隧道模式分别验证 capability、CSP、外链和诊断命令。
- 补齐 `Cargo.lock`、安装包签名、发布版本和升级/回滚流程。

### 产品安全政策

旧 Chloe 模板允许露骨性对话和性需求真人匹配，而旧计划曾写“禁止成人内容”，旧模板还包含不披露 AI 身份的要求。2026-09-05 的独立分身聊天入口已改用明确 AI 身份的新模板，旧模板保留用于兼容；其余内容政策仍需统一。产品在扩大范围前必须明确并实现：

- AI 身份披露原则。
- 年龄门槛和未成年人隔离。
- 成人内容是否允许，以及双方偏好与同意模型。
- 自伤、危机、骚扰、胁迫和非自愿内容的确定性处理流程。
- 哪些安全决策必须由代码/审核完成，而不能只依赖提示词。

完成标准：产品规则、用户条款、Persona、匹配逻辑和审核代码不存在互相矛盾。

## P1：近期迭代

### 身份与隐私

- 为全部上传文件增加保留期限和全目录孤儿扫描；队列内失败项的周期重试已完成。
- 为历史上无法通过现有匿名标识回填 owner 的广场帖子制定人工归属/清理策略。

### 输入、成本和并发

- 继续为尚未覆盖的昂贵模型接口增加按用户限流、并发限制和取消处理；客户端超时/有限重试已统一。
- 为公共热点和后台画像/匹配任务设置缓存、队列和并发边界。

### 本轮明确暂缓

按当前产品决定，以下两项不进入本轮实现，也不应被描述为已经完成：

- 草莓余额并发检查与扣减的原子性。
- 将模型预算、真实 usage 和费用上限写入持久化存储。

### 前端

- 清理未使用状态、Effect 依赖和原生 `<img>` 警告。
- 将主页面按聊天、语音、匹配、工具卡片和抽屉拆分。
- 抽屉首次打开时再挂载 iframe，关闭时暂停轮询和 3D 渲染。
- 麦克风权限只在用户动作后请求。
- 恢复可缩放 viewport，并继续支持 reduced motion。

### 依赖

- 为 Python 生成可复现 constraints/lock，并提交桌面 `Cargo.lock`；前端与桌面 npm lockfile 已完成。

## P2：工程治理

- 推送后确认新增 CI 在 GitHub runner 全部通过，并把质量门禁设为 `main` 的 required checks。
- 增加前端组件测试和关键用户旅程 E2E。
- 处理 SlowAPI/Starlette/httpx 弃用警告。
- 给 ASR 大字节参数增加短测试 ID，避免 pytest cache 膨胀。
- 引入正式数据库迁移、SQLite WAL/busy timeout 和一致的事务边界。
- 用户量和并发增长前评估 PostgreSQL；当前仓库尚无 PostgreSQL 实现，不能称为“无缝迁移”。
- 增加结构化日志、错误追踪、健康检查、外部依赖监控和费用告警。
- 清理会全局结束 Python/Node 进程的旧 Windows 启动脚本，仅保留按 PID/子进程管理的入口。

## 文档与运维

当前文档入口：

- [README.md](./README.md)：项目说明、本地启动和检查命令。
- [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md)：实际模块、数据流和边界。
- [docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md)：Nginx、systemd、备份、更新和回滚。
- [frontend/README.md](./frontend/README.md)：Web 开发说明。
- [desktop/README.md](./desktop/README.md)：Windows 桌面开发和构建。

后续每次修改模型、路由、环境变量、端口、部署方式、数据位置或发布安全结论时，必须在同一个变更中更新对应文档。

## 下一里程碑建议

### M1：安全的小范围内测基线

- 已完成桌面命令边界、SSRF、Layer 2 双边匹配和首批输入大小/限流修复。
- 已完成会话撤销、邀请码管理、完整账户删除和跨用户匹配数据最小化。
- 完成桌面 Windows/Rust 发布验证。
- 补齐剩余模型并发和取消边界；持久化成本项按当前决定暂缓。
- 完成成人内容、年龄和危机处理产品决策。
- 推送并确认后端测试、TypeScript、Lint、生产构建和依赖审计在 CI 通过。

### M2：可运营的封闭测试

- 对可撤销邀请码/会话和完整账户删除执行真实浏览器 E2E 与恢复演练。
- 上传文件完整生命周期、数据库备份演练和结构化监控。
- 关键登录、聊天、匹配、Peer 房间 E2E。

### M3：公开产品评估

- 在完成隐私、安全、内容治理和运营能力后，再决定是否开放注册。
- 根据真实用户量和 SQLite 指标决定是否迁移 PostgreSQL，而不是预先承诺无缝扩容。
- 桌面安装包增加签名、更新机制和发布验证流程。
